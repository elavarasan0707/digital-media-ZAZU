rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isAdmin() {
      return isAuthenticated() && (
        request.auth.token.email == 'digitalmediazazu@gmail.com' ||
        request.auth.token.email == 'eladigitalw@gmail.com' ||
        request.auth.token.email == 'elae2379@gmail.com'
      );
    }

    // Site configuration (read by everyone, updated only by admin)
    match /site_config/{configId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Works / Case Studies (read by public, create/update/delete only by admin)
    match /works/{workId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Reviews (read by public, create/update/delete only by admin)
    match /reviews/{reviewId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Services (read by public, create/update/delete only by admin)
    match /services/{serviceId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Client Inquiries / Contact Messages (create by public, read/delete by admin)
    match /inquiries/{inquiryId} {
      allow create: if true;
      allow read, update, delete: if isAdmin();
    }

    // Booked slots / Consultations (read by public to display availability, create by public, manage by admin)
    match /bookings/{bookingId} {
      allow read, create: if true;
      allow update, delete: if isAdmin();
    }

    // User profiles (users can read/write their own profile, admin can read all)
    match /users/{userId} {
      allow read, write: if isAuthenticated() && (request.auth.uid == userId || isAdmin());
    }
  }
}
